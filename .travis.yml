sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t aishahsofea/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aishahsofea/docker-react npm run test
